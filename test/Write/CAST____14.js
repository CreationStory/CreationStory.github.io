window.addEventListener('load', (event) => { const CAST14 = document.querySelector('.CAST______________'); const CAST = document.querySelector('.CASTVECTOR'); const performActions = () => {if (CAST14) { CAST14.style.opacity = '0'; if (CAST) { CAST.style.backdropFilter = 'blur(0px)'; CAST.style.opacity = '0'; } }}; const maxLoadingTime = 7400; let timeoutId = setTimeout(() => { performActions(); }, maxLoadingTime); window.addEventListener('load', () => { clearTimeout(timeoutId); }); const retryDelay = 1400; const maxRetries = 2; let retries = 0; const errorHandler = () => {retries++; if (retries <= maxRetries) { setTimeout(() => { performActions(); }, retryDelay); } else { performActions(); }}; try {performActions();} catch (error) {errorHandler();}});